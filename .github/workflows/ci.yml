# .github/workflows/ci.yml
# CI Pipeline: Lint, Validate Notebooks
#
# トリガー:
# - main/developブランチへのpush
# - Pull Request
# - 手動実行（workflow_dispatch）

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

# 同一ブランチでの重複実行を防ぐ
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.12"
  UV_SYSTEM_PYTHON: 1

jobs:
  # ============================================================
  # Lint Job: Ruff による静的解析
  # ============================================================
  lint:
    name: 🔍 Lint (Ruff)
    runs-on: ubuntu-24.04
    timeout-minutes: 5 # ワークフローのタイムアウトを5分に設定
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 🚀 Install uv
        uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081 # v5

      - name: 📦 Install dependencies
        run: |
          uv sync

      - name: 🔍 Run Ruff linter
        run: |
          uv run ruff check --output-format=github .

      - name: 🎨 Run Ruff formatter (check mode)
        run: |
          uv run ruff format --check .

  # ============================================================
  # Notebook Validation: marimo ノートブックの構文チェック
  # ============================================================
  validate-notebooks:
    name: 📓 Validate Notebooks
    runs-on: ubuntu-24.04
    timeout-minutes: 5 # ワークフローのタイムアウトを5分に設定
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: 🚀 Install uv
        uses: astral-sh/setup-uv@e58605a9b6da7c637471fab8847a5e5a6b8df081 # v5

      - name: 📦 Install dependencies
        run: |
          uv sync

      - name: 📓 Validate marimo notebooks syntax
        run: |
          # marimoノートブックがPythonとして有効かチェック
          for notebook in notebooks/*.py; do
            echo "Validating $notebook..."
            uv run python -m py_compile "$notebook"
          done

      - name: 🔍 Check notebooks with Ruff
        run: |
          uv run ruff check notebooks/
